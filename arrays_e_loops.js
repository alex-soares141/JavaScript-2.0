// incrementando de 1 em 1
for (var teste = 1; teste <=10; teste++){
    console.log(teste)
}


var VideoGames = ['swithc', 'ps4', 'xbox', 'nintendo', 'playstation', 'nintendo'];

for (var game = 0; VideoGames.length <=5; game++){
    console.log(VideoGames[game])
} 

var frutas = ['banana', 'maca', 'pera' ];
{
    frutas.forEach(function(fruta, index) {
        console.log(fruta, index)
    })
}

//Exercicio

var copa = ['Alemanha', 'Brasil', 'Egito', 'Uruguai', 'Portugal', 'Italia'];

for (var i = 0; i < copa.length; i++) {
    console.log(copa[i]);
}